{
  "name": "pclaude-installer",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop installer and launcher for local Claude Code.",
  "author": "DPCC",
  "main": "dist-electron/main/index.js",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite --config vite.renderer.config.ts",
    "dev:app": "node scripts/dev-electron.mjs",
    "dev:electron": "sh -c 'vite --config vite.main.config.ts build --watch --mode electron-main & vite --config vite.main.config.ts build --watch --mode electron-preload & wait'",
    "build": "vite --config vite.renderer.config.ts build && vite --config vite.main.config.ts build --mode electron-main && vite --config vite.main.config.ts build --mode electron-preload",
    "start": "npm run build && electron .",
    "package": "npm run build && electron-builder --publish never",
    "package:dir": "npm run build && electron-builder --dir --publish never",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.0.0",
    "@types/node": "^24.0.0",
    "@types/react": "^19.1.2",
    "@types/react-dom": "^19.1.2",
    "@vitejs/plugin-react": "^5.0.0",
    "electron": "^37.10.3",
    "electron-builder": "^26.8.1",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
